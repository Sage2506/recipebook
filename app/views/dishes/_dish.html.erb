<div id="<%= dom_id dish %>">

  <p class="my-5">
    <ul>
      <% if dish.image%>
        <li>
          <% if dish.image.representable? %>
          <div class="">can show image</div>
            <%= image_tag dish.image.representation(resize_to_limit: [150, 150]) %>
            <%= link_to rails_blob_path(dish.image, disposition: "attachment") do %>
              <div class="">download file</div>
            <% end %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </p>
  <p class="my-5">
    <strong class="block mb-1">Name:</strong>
    <%= dish.name %>
  </p>

  <p class="my-5">
    <strong class="block mb-1">Description:</strong>
    <%= dish.description %>
  </p>

  <% if action_name != "show" %>
    <%= link_to "Show this dish", dish, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to 'Edit this dish', edit_dish_path(dish), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
